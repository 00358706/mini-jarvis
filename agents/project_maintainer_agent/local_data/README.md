# local_data

Optional **static reference material** for maintainers using this agent configuration: notes, checklists, links to ADRs, or small non-secret snippets.

- Content here is **advisory**; it is not loaded by the gateway at runtime today.
- Do not store API keys, tokens, or production secrets.
- Prefer pointers to issues or docs over duplicating large specs.
